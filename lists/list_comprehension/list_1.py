name = "saul"

print([i.upper() for i in name])

colors = ["orange", "blue", "green", "red", "pink"]

# this will bring back the item in the list
print([color.upper() for color in colors])


print([color[2].upper() for color in colors])
# this will bring back the second index
# of each item in the list
